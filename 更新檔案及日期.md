ROY 6/5號更新git
完成工作計劃書
完成前端2頁(首頁/錢包)

--------------------------------
製作日期及時程表:
6/2欄位製作完成
6/3開始製作前端
6/9製作前端頁面完成 (多頁)

6/10開始後端工作
 |
6/21製作api後端完成

6/23整合

6/24-27整合後修改(對齊所有人api)


6/28更新做修改的地方

Position 加上 (user_id, market_id) 唯一約束
檔案：[Position.java (line 10)]

避免同一個使用者在同一個市場被建立多筆持倉。只能有一筆資料,避免重複持倉
----------------------------------------
PositionRepository 加 DB 原子 upsert
檔案：[PositionRepository.java (line 29)]


用 PostgreSQL INSERT ... ON CONFLICT DO UPDATE 直接累加 yes/no shares 和 cost，解掉 read-then-write 併發覆蓋問題。
買 YES 時累加 yes_shares / yes_cost
買 NO 時累加 no_shares / no_cost
賣出時用 lock 查資料，避免同時賣出扣錯
沒有 position 就新增 已經有 position 就累加
-----------------------------------------------------
PositionService.addBuyPosition() 改成呼叫 upsert
檔案：[PositionService.java (line 79)]
買入時不再先查再改，而是讓 DB 保證原子性。sellPosition() 也改成用 pessimistic lock 查持倉，避免賣出併發扣錯。
加了下面這些:

-----------------------------------------------------------
我這次先修 position 持倉資料一致性；權限問題還沒修，等 JWT/Security 完成後，要把 /api/positions/user/{userId} 改成 /api/positions/me。

現在修改的是:
買入後 position 正確累加
同 user + 同 market 不會產生多筆
同時買入不會覆蓋 shares

如果userid換成其他人的就可能查到別人持倉
有權限的是這兩隻
GET localhost:8080/api/positions/user/11111111-1111-1111-1111-111111111111/open
GET localhost:8080/api/positions/user/11111111-1111-1111-1111-111111111111

所以有整合登入的時候再換這兩隻
要帶入token
(Authorization: Bearer 你的accessToken)

GET http://localhost:8080/api/positions/me
GET http://localhost:8080/api/positions/me/open
GET /api/positions/me
GET /api/positions/me/open
不要讓前端傳 userId，而是從登入 token 判斷目前使用者是誰。


等整合後positioncontroller.java改 然後api換成上面那兩隻 (要帶token) 就不會抓到其他人的

package com.ucmarket.controller;

import java.util.List;

import org.springframework.security.core.annotation.AuthenticationPrincipal;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.ucmarket.dto.PositionResponse;
import com.ucmarket.entity.User;
import com.ucmarket.service.PositionService;

@RestController
@RequestMapping("/api/positions")
public class PositionController {

    private final PositionService positionService;

    public PositionController(PositionService positionService) {
        this.positionService = positionService;
    }

    @GetMapping("/me")
    public List<PositionResponse> getMyPositions(@AuthenticationPrincipal User user) {
        return positionService.getPositionsByUserId(user.getId());
    }

    @GetMapping("/me/open")
    public List<PositionResponse> getMyOpenPositions(@AuthenticationPrincipal User user) {
        return positionService.getOpenPositionsByUserId(user.getId());
    }
}