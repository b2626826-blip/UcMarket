# n8n 整合線——開發進度

更新：2026-07-20｜負責：Harry｜維護規則：**關卡狀態變動時，本檔與該次 commit 一起更新**

現況一句話：Java WP0–WP5、`N8nWebhookEmailSender`、`04-notify-webhook`、`05-failed-alert` 正式排程與災難復原 runbook 均已完成 runtime 驗收；`07-resolution-evidence-collector` 已完成第一輪獨立驗收 findings 修復，待修復後 fresh session runtime 複驗，`02`／`03` 不存在。

## ✅ 已完成（含驗收證據）

| 項 | 內容 | 驗收 |
|---|---|---|
| 環境 | Docker compose（n8n `2.29.11`＋mailpit `v1.30.4`，版本釘死）；win/mac 同一份；一鍵腳本（分步顯示＋原生下載進度＋失敗不閃退＋就緒探測逐秒顯示、轉發悶住自動救援＋完成自動開頁＋全新安裝自動匯入**憑證＋workflows**）＋手動雙軌安裝文件；憑證檔進 repo（低敏感開發憑證，使用者核可，公開前輪替） | 兩容器 Up、`/healthz` OK；Windows 實測綠（mac 待隊友首跑回報）；**新機器全流程已隔離實測**（憑證 3 筆＋workflow 4 條、照 id 覆蓋不長重複、匯入後停用、綁定自動生效） |
| Discord 通道 | `#通知`／`#心跳` 兩頻道、webhook 憑證入 n8n 憑證庫 | 頻道即時收到測試訊息 |
| `01-health-alert` 健康告警 | 每 10 秒探測；連 3 次失敗🔴（含已躺時長）、掛超過 30 分重提醒、復活🟢（含停機時長）；安靜＝正常 | 實測：關後端約 40 秒一則🔴、重啟一則🟢、無連環轟炸 |
| `06-heartbeat` 心跳 | 整點／半點💓，後端活著才跳（💓消失＝有事）；獨立頻道 | 排程實測 |
| `04-notify-webhook` ⭐ | `POST /webhook/notify` 契約端點：token 通過且真寄出才 200；未授權 403、缺欄 400；開發期以 2 秒 Mailpit preflight 讓停機直接 500 且不進 Email；單純轉發不碰語意 | **四連測全實證**：200＋信入 Mailpit／缺與錯 token 皆 403 且不增信／400 且不增信／stop Mailpit→0.085 秒回 500、start 後等待 5 秒仍無延遲寄信 |
| Java WP0–WP5 | outbox、Worker、重試／冪等、送審與六類事件通知、WP4 管理員查詢／重送 | WP5 commit `327d9fe`；定向與 backend 全測試、獨立驗收已完成 |
| `N8nWebhookEmailSender`＋webhook token | Java POST JSON＋`X-Webhook-Token`；URL／token／connect/read timeout 可設定；n8n Header Auth 在 Email 前拒絕缺少／錯誤 token | sender／Worker 定向測試通過；實跑 200／403／400／500；n8n 停機 attempt #1=`RETRY`、恢復 attempt #2=`SENT`，Mailpit 恰好一封且收件人／主旨／本文符合模板 |
| n8n 查詢專用 service token | 僅允許 `X-N8N-Service-Token` 呼叫 `GET /api/admin/notifications?status=FAILED`；不能查其他 status、resend 或其他 `/api/admin/**` | commit `61f7b65`；backend 全測試 349 tests、0 failures、0 errors、15 skipped；獨立驗收完成 |
| `05-failed-alert` FAILED 告警 | 正式每 5 分鐘完整分頁查詢；以 `jobId → attemptCount` 防重複；每輪最多一則 Discord。backend 存活告警由 `01-health-alert` 負責；`05` 遇停機、連線失敗或 5xx 不發 Discord、不提交 staticData | n8n `2.29.11` runtime：105 筆 FAILED 完整讀取第二頁；相同快照不重複，attemptCount 增加會再次告警；401／403／400 明確失敗且不更新 staticData；Discord 失敗不提交，恢復後下一輪成功發送並只提交一次；驗收無 findings |
| `automation/n8n/runbook.md` | 災難還原五步、憑證重建、告警位置與演練流程 | 已完成並納入主線收尾 |

## 🟡 已實作、待獨立 runtime 驗收

| 項 | 內容 | 待驗證 |
|---|---|---|
| `07-resolution-evidence-collector` | 每 60 分鐘讀取最多 4 頁／200 筆時事市場候選，先完成分頁快照再逐筆保存 `candidate.sourceUrl`；以 Code sandbox 相容 parser 產生 hostname `sourceTitle`、`publishedAt=null`；兩個 write credential HTTP 節點間以顯式 retry 迴圈完成最多 3 次 POST；成功／失敗／手動 execution 設為不保存，預設 pruning 下先 soft-delete、再由 hard-prune 清除實體列 | n8n `2.29.11` 修復後複驗：兩 credential 交叉 403、52／202 筆分頁上限、hostname／`publishedAt` 聚合、暫時性錯誤第三次成功、永久 4xx 不重試、分頁失敗零寫入、重跑冪等；execution 驗收以 `deletedAt IS NULL` 為零判定；CLI raw output 不得落地 |

## ⬜ 未實作

| 項 | 內容 | 前置條件 |
|---|---|---|
| LINE 通道 | 告警＋摘要推 LINE 群 | 選配：主線完工且 demo 前有餘裕才做 |

## 🚫 已取消／不重複實作

| 項 | 原因 |
|---|---|
| `/api/internal/notifications/digest`＋`02-daily-digest` | WP5 已由 Java 每日 09:00 建立 `DAILY_PENDING_REVIEW_SUMMARY` outbox 工作 |
| `/api/internal/notifications/closing-markets`＋`03-closing-reminder` | WP5 已由 Java 每小時建立 `MARKET_CLOSING_REMINDER` outbox 工作 |

## 🔗 跨線協作事項

| 事項 | 所屬 | 狀態 |
|---|---|---|
| `N8nWebhookEmailSender`＋sender 設定鍵 | 核心線 | **已完成** |
| `04-notify-webhook` token 驗證與契約文件 | 整合線 | **已完成**；秘密只存各 n8n instance credential store |
| service token＋`05-failed-alert` | 核心線＋整合線 | **已完成**；service token、credential 綁定、正式排程與 runtime 驗收均完成 |
