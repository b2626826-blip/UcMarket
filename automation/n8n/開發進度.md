# n8n 整合線——開發進度

更新：2026-07-16｜負責：Harry｜維護規則：**關卡狀態變動時，本檔與該次 commit 一起更新**

現況一句話：環境、監控告警、通知收信端（webhook 契約端點）已上線並完成驗收；共用檔提案核准後，隨即進入 Java 關卡。

## ✅ 已完成（含驗收證據）

| 項 | 內容 | 驗收 |
|---|---|---|
| 環境 | Docker compose（n8n `2.29.11`＋mailpit `v1.30.4`，版本釘死）；win/mac 同一份；一鍵腳本（分步顯示＋原生下載進度＋失敗不閃退＋就緒探測逐秒顯示、轉發悶住自動救援＋完成自動開頁＋全新安裝自動匯入**憑證＋workflows**）＋手動雙軌安裝文件；憑證檔進 repo（低敏感開發憑證，使用者核可，公開前輪替） | 兩容器 Up、`/healthz` OK；Windows 實測綠（mac 待隊友首跑回報）；**新機器全流程已隔離實測**（憑證 3 筆＋workflow 3 條、照 id 覆蓋不長重複、匯入後停用、綁定自動生效） |
| Discord 通道 | `#通知`／`#心跳` 兩頻道、webhook 憑證入 n8n 憑證庫 | 頻道即時收到測試訊息 |
| `01-health-alert` 健康告警 | 每 10 秒探測；連 3 次失敗🔴（含已躺時長）、掛超過 30 分重提醒、復活🟢（含停機時長）；安靜＝正常 | 實測：關後端約 40 秒一則🔴、重啟一則🟢、無連環轟炸 |
| `06-heartbeat` 心跳 | 整點／半點💓，後端活著才跳（💓消失＝有事）；獨立頻道 | 排程實測 |
| `04-notify-webhook` ⭐ | `POST /webhook/notify` 契約端點：真寄出才 200、缺欄 400、寄失敗 500；單純轉發不碰語意 | **三連測全實證**：200＋信入 mailpit／400／stop mailpit→500、start→200 |

## ⬜ 未實作

| 項 | 內容 | 前置條件 |
|---|---|---|
| Java 唯讀 API ×2 | `/api/internal/notifications/digest`＋`/closing-markets`＋`ServiceTokenFilter`（X-Service-Token） | **SecurityConfig＋properties 兩行提案核准** |
| `02-daily-digest` 每日摘要 | 08:00 拉 digest → Discord embed | 上面的 Java API |
| `03-closing-reminder` 截止提醒 | 每小時拉 closing-markets → 彙總一則發 DC | 上面的 Java API |
| `05-failed-alert` FAILED 告警 | 輪詢 `GET /api/admin/notifications?status=FAILED` | 核心線 WP4 合入 |
| runbook.md | 災難還原五步＋憑證重建＋告警去哪看 | 主線收尾時寫＋演練 |
| LINE 通道 | 告警＋摘要推 LINE 群 | 選配：主線完工且 demo 前有餘裕才做 |

## 🔗 跨線協作事項

| 事項 | 所屬 | 狀態 |
|---|---|---|
| SecurityConfig＋application.properties 各加一行（service token 提案） | 共用檔（全隊） | 擬稿已備，**未送** |
| 貼 30 行 `N8nWebhookEmailSender` adapter（參考實作已附對接文件） | 核心線 | 對接文件待交付 |
| WP4 admin API（FAILED 告警的資料源） | 核心線 | 尚未合入 |
