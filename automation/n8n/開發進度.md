# n8n 整合線——開發進度

更新：2026-07-19｜負責：Harry｜維護規則：**關卡狀態變動時，本檔與該次 commit 一起更新**

現況一句話：雙線同日收工——核心線 WP0–WP5＋`N8nWebhookEmailSender`＋`04` token 驗證完成；整合線關卡 3 API ×3＋service token＋`02`/`03`/`05` 上線驗收完成。剩：**兩線功能重疊的分工對齊**（見 ⚠️ 節）＋runbook。

## ✅ 已完成（含驗收證據）

| 項 | 內容 | 驗收 |
|---|---|---|
| 環境 | Docker compose（n8n `2.29.11`＋mailpit `v1.30.4`，版本釘死）；win/mac 同一份；一鍵腳本（分步顯示＋原生下載進度＋失敗不閃退＋就緒探測逐秒顯示、轉發悶住自動救援＋完成自動開頁＋全新安裝自動匯入**憑證＋workflows**）＋手動雙軌安裝文件；憑證檔進 repo（低敏感開發憑證，使用者核可，公開前輪替） | 兩容器 Up、`/healthz` OK；Windows 實測綠（mac 待隊友首跑回報）；新機器全流程已隔離實測 |
| Discord 通道 | 五頻道分流：`#通知`（01）／`#心跳`（06）／`#每日摘要`（02）／`#市場關閉`（03）／`#寄信狀態`（05），webhook 憑證全數入 n8n 憑證庫與 `install/credentials.json`（7 筆） | 各頻道即時收到對應訊息（07-19 實測） |
| `01-health-alert`／`06-heartbeat` | 告警（連 3 失敗🔴、30 分重提醒、復活🟢）／整點半點💓 | 實測綠（07-16） |
| `04-notify-webhook` ⭐ | `POST /webhook/notify` 契約端點：**先驗 `X-Webhook-Token`**（缺／錯 403 且不進 Email）→ 驗必填（400）→ 2 秒 Mailpit preflight（停機直接 500）→ 真寄出才 200；單純轉發不碰語意 | 核心線**四連測全實證**：200＋信入 Mailpit／403 ×2／400／stop Mailpit→0.085s 回 500；RETRY→SENT 重試路徑實測 |
| Java WP0–WP5（核心線） | outbox、Worker、重試／冪等、送審與六類事件通知、WP4 管理員查詢／重送 | WP5 commit `327d9fe`；backend 全測試＋獨立驗收完成 |
| `N8nWebhookEmailSender`（核心線） | Java POST JSON＋`X-Webhook-Token`；URL／token／timeout 全可設定、fail-fast 驗證；與 `notification.worker.enabled` 綁同開關 | sender／Worker 定向測試通過；07-19 整合線實跑：送審 → 60 秒內 mailpit 收信（六類事件信全數實測入匣） |
| 關卡 3 Java（整合線 07-19） | `GET /api/internal/notifications/digest`／`closing-markets`／`failed` ×3 唯讀 API＋`ServiceTokenFilter`（X-Service-Token、fail-closed、常數時間比較）；SecurityConfig 放行一行＋`service.token` 一行已合入 | 13 測試綠（contract 9＋H2 整合 4）；curl 實測 401/401/200 |
| `02`/`03`/`05` workflow（整合線 07-19） | 每日摘要（08:00 單發＋`lastSentDate` 判重保險）／截止提醒（每 30 分、`sentKeys` 防重彙總一則）／FAILED 告警（每 10 分、count 變化才發）；憑證綁 `ucmarket-service-token`（id 固定，新機器自動綁定） | **DC 實測全綠**（07-19 13:00 首發 📊＋⏰ 2 檔＋⚠️ 1 筆；防重、增量、SYS- 排除皆驗） |

## ⚠️ 兩線重疊待對齊（07-19 發現，下次同步拍板）

核心線 WP5 的 outbox 事件與整合線 `02`/`03` 功能有重疊，核心線文件曾建議取消整合線版本；但兩套**受眾與通道不同**、07-19 已實測**並行運作互不干擾**（同時段 mailpit 收 email、DC 收廣播）：

| 核心線（email 個人信，走 outbox→04） | 整合線（Discord 頻道廣播，直發） | 重疊度 |
|---|---|---|
| `DAILY_PENDING_REVIEW_SUMMARY`（09:00 寄管理員） | `02` 每日摘要（08:00 發 DC，含成交量等營運數字） | 中——都是管理員日報，內容不同 |
| `MARKET_CLOSING_REMINDER`（寄持倉使用者） | `03` 截止提醒（發 DC 社群廣播） | 低——受眾明確不同 |

選項：A 並存（現狀，各養各的）／B 收斂單邊。**待兩線討論後更新本節**；在那之前兩套都維持上線。

## ⬜ 未完成

| 項 | 內容 | 前置條件 |
|---|---|---|
| runbook.md | 災難還原五步＋憑證重建＋告警去哪看 | 主線收尾時寫＋演練 |
| LINE 通道 | 告警＋摘要推 LINE 群 | 選配：主線完工且 demo 前有餘裕才做 |

## 🔗 跨線協作事項

| 事項 | 所屬 | 狀態 |
|---|---|---|
| SecurityConfig＋application.properties（service token 兩行） | 共用檔（全隊） | ✅ 已合入（07-19；token 走開發預設值，正式部署 env 覆蓋）；告知文件《2026-07-19-共用檔提案-service-token》 |
| `N8nWebhookEmailSender`＋sender 設定鍵 | 核心線 | ✅ 已完成（正式版取代整合線 07-19 的過渡版） |
| `04` token 驗證與契約文件 | 核心線改造＋整合線收錄 | ✅ 已完成；`ucmarket-notify-webhook-token` 秘密只存各 n8n instance credential store，**不進 repo** |
| `05` FAILED 告警資料源 | 整合線自建 `GET /api/internal/notifications/failed` | ✅ 已完成上線——admin 面不動、不擴 token 範圍（對外版 v1.4 已註記；核心線原規劃的 admin API 輪詢不再需要） |
