# 這些檔案會被 scp 到 Linux VM 上執行或掛載，必須是 LF。
# Windows checkout 預設 (core.autocrlf=true) 會轉成 CRLF，bash 會把行尾的 CR
# 當成指令的一部分——2026-08-24 實測 render-runtime-secrets.sh 與兩個 Caddyfile
# 都是這樣傳上 VM 的。
*.sh        text eol=lf
*.yml       text eol=lf
Caddyfile*  text eol=lf
